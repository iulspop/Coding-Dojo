const answer = require('./leap_year');
const leapYear = require('./leap_year');

describe('leapYear', () => {
  it('to life the universe and everything', () => {
    expect(leapYear()).toEqual(42);
  });
});
